$(function() {
  $(".p2").typed({
    strings: ["FAMILY", "COWORKERS", "FRIENDS", "SITES"],
    typeSpeed: 80,
    backSpeed: 10,
    backDelay: 1800,
    showCursor: false,
    loop: false
  });
});
